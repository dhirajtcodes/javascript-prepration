// requestAnimationFrame() is a method that tells the browser
// you want to perform an animation and requests the browser to call
// a specified function before the next repaint (typically synced with a
// 60Hz refresh rate, i.e., ~60 frames per second).
